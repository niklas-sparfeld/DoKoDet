{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://doko-detector.local/schemas/annotation-v1.schema.json",
  "title": "CardEventNet video annotation v1",
  "type": "object",
  "additionalProperties": false,
  "required": ["video", "roi", "events"],
  "properties": {
    "video": {
      "type": "string",
      "minLength": 1
    },
    "roi": {
      "type": "object",
      "additionalProperties": false,
      "required": ["x", "y", "width", "height"],
      "properties": {
        "x": {"type": "number", "minimum": 0, "maximum": 1},
        "y": {"type": "number", "minimum": 0, "maximum": 1},
        "width": {"type": "number", "exclusiveMinimum": 0, "maximum": 1},
        "height": {"type": "number", "exclusiveMinimum": 0, "maximum": 1}
      }
    },
    "events": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/event"
      }
    }
  },
  "$defs": {
    "event": {
      "type": "object",
      "additionalProperties": false,
      "required": ["time_s", "type"],
      "properties": {
        "time_s": {
          "type": "number",
          "minimum": 0
        },
        "type": {
          "enum": [
            "card_played",
            "trick_cleared",
            "card_moved",
            "card_removed",
            "card_returned",
            "multiple_cards_dropped",
            "anomalous_state_change"
          ]
        },
        "confidence": {
          "enum": ["confirmed", "uncertain", "ignore", "proposed"]
        },
        "notes": {
          "type": ["string", "null"]
        }
      }
    }
  }
}
